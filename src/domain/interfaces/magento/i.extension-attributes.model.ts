import { BundleProductOptionModel, CategoryLinkModel } from '.';

export interface ExtensionAttributeModel {
  bundle_product_options: BundleProductOptionModel[];
  category_links: CategoryLinkModel[];
}
